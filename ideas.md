- Función de similitud vectorial -> Distancia coseno (argumentar por qué)
- Embeddings -> Lo mejor sería fine-tunear un modelo para que se especialice en hacer embeddings de animales
- NO usar notebooks (sólo para hacer pruebas si acaso), usar clases, estructurarlo en distintos ficheros, etc.
- Probar los retrievers sueltos en aislamiento para comprobar si el chunking está funcionando correctamente o devuelve mucha "paja"
- Reranking usando un LLM para mirar los top-k devueltos y decidir cuál es el mejor o reordenarlos de nuevo por si acaso el score anterior no es el mejor. Esto se usa si hay mucha competencia entre chunks muy parecidos (score muy parecido).
- Probar con preguntas mal hechas por el usuario "Qué obras de comedia escribió Rafa Nadal?"
- Hacer que en la base de datos cada nodo Chunk tenga una o varias relaciones HAS_QUESTION que apunten a nodos Question que contengan cada uno una pregunta a la que dicho chunk responde así como el embedding de la pregunta. De esta forma los chunks son en realidad las preguntas a las que responde y el embedding de cada una, así se compara el embedding de la pregunta del usuario con los embeddings de las preguntas a las que responde cada chunk.
- En el chunking también se puede pedir a un LLM que reprocese cada chunk dividiendolo más antes de guardarlo en la base de datos, así se reduce el ruido y cada chunk pequeño habla de un mismo tema y no varios. De esta forma, cada chunk original tiene varias relaciones HAS_CHILD unidos a nodos Hijo que tienen una pequeña parte del chunk original y el embedding de esta parte pequeña del chunk.
- Probar con preguntas de tipo "Dime los tres animales que vivan en la Savana y sean depredadores de cebras" que requieran few shot y que el LLM tenga acceso a nuestra base de datos para que él tenga que hacer una consulta cypher a nuestra base de datos.
- Usar terminology mapping e instrucciones de formato para la respuesta